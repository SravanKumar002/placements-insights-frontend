# placements-insights-frontend

Vite + React app for Placement Insights. Install from monorepo root with `npm install`, then `npm run dev -w placement-web`, or from this folder: `npm install && npm run dev`.
